version: "3.8"
services:
  olx-monitor:
    build: .
    restart: unless-stopped
    deploy:
      replicas: 3
    volumes:
      - ./src/config:/usr/app/config
      - ./src/.env:/usr/app/.env
      - ./data:/usr/data

